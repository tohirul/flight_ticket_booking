
model Airport {
    id String @id @default(cuid())
    name String
    code String
    city String
    country String
    lat     Float
    lng     Float
    timezone String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    
}